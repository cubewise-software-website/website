{
  "name": "cubewise-website",
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "build": "node scripts/build.js",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "dependencies": {
    "cheerio": "1.0.0"
  },
  "devDependencies": {
    "vitest": "^2.0.0"
  }
}
